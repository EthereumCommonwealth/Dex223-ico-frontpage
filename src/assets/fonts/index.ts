import { Golos_Text } from "next/font/google";

// export const robotoFlex = Roboto_Flex({ subsets: ['latin'] });
// export const saira = Saira({subsets: ['latin']});
export const golos_text = Golos_Text({ subsets: ['latin'] });
